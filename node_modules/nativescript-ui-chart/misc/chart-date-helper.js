export function getCalendarFromDate(date) {
    let calendar = java.util.Calendar.getInstance();
    calendar.setTimeInMillis(date.getTime());
    return calendar;
}
export function getDateFromCalendar(calendar) {
    return new Date(calendar.getTimeInMillis());
}
export function getCalendarFromMilliseconds(value) {
    let calendar = java.util.Calendar.getInstance();
    calendar.setTimeInMillis(value);
    return calendar;
}
//# sourceMappingURL=chart-date-helper.js.map